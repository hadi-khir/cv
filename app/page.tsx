import Image from "next/image";

export default function Home() {
  return (
    <div className="min-h-screen m-8 flex flex-col items-center bg-green-50">

      {/* header section */}
      <header className="font-[family-name:var(--font-geist-mono)] sticky top-0 left-0 w-full z-10 flex justify-between p-4">
        <button className="text-gray-800 dark:text-white">menu</button>
        <button className="text-gray-800 dark:text-white">github</button>
      </header>

      {/* image section */}

    </div>
  );
}
